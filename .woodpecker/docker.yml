  when:
    event: [push, manual, cron]
    branch: main

  steps:
  - name: build
    image: woodpeckerci/plugin-docker-buildx:6
    settings:
      repo: alastairhm/alpine-builder
      username:
        from_secret: docker_username
      password:
        from_secret: docker_token
      tag: latest
